<UserControl x:Class="AdvertManager.Client.Views.NewspaperAdvertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="850">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding NewspaperAdsView}"
                  IsReadOnly="True"
                  Margin="10"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*" />
                <DataGridTextColumn Header="Publisher" Binding="{Binding PublisherFullName}" Width="2*" />
                <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="1" Margin="5">
            <Button Content="Add" Command="{Binding AddEntityCommand}" Margin="0,5"/>
        </StackPanel>
    </Grid>
</UserControl>
