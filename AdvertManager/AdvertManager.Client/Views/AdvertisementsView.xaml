<UserControl x:Class="AdvertManager.Client.Views.AdvertisementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdvertManager.Client.Views"
             xmlns:viewModels="clr-namespace:AdvertManager.Client.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="850">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0"
                 Margin="10"
                 VerticalAlignment="Center"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

        <DataGrid Grid.Row="1" Grid.Column="0"
          AutoGenerateColumns="False"
          ItemsSource="{Binding AdvertisementsView}"
          SelectedItem="{Binding SelectedAdvertisement}"
          IsReadOnly="True"
          Margin="10"
          SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="30"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="Auto" />
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="Auto" />
                <DataGridTextColumn Header="Expires" Binding="{Binding ExpirationDate}" Width="Auto" />
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher.Id}" Width="*" />
                <DataGridTextColumn Header="Real Estate" Binding="{Binding RealEstate.Id}" Width="*" />
                <DataGridTextColumn Header="State" Binding="{Binding StateName}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>


        <StackPanel Grid.Column="1" Grid.Row="1" Margin="5, 0, 15, 0">
            <Button Content="Add" Command="{Binding AddEntityCommand}" Margin="0,5"/>
            <Button Content="Update" Command="{Binding UpdateEntityCommand}" Margin="0,5"/>
            <Button Content="Remove" Command="{Binding RemoveEntityCommand}" Margin="0,5"/>
            <Button Content="Redo" Margin="0,5"/>
            <Button Content="Undo" Margin="0,5"/>
        </StackPanel>
    </Grid>
</UserControl>